/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};
module.exports = {
  output: 'export',
  basePath: '/DDay',
  assetPrefix: '/DDay',
};

export default nextConfig;
